# LoreMonkey — downloads

**[⬇ Download LoreMonkey for Windows](../../releases/latest)**

An AI copilot that listens to your D&D session and hands you in-character
suggestions at the right moment. Audio and transcription run entirely on your own
machine.

## Installing

1. Download `LoreMonkey_x.y.z_x64-setup.exe` from the
   [latest release](../../releases/latest).
2. Run it. Windows will warn that the publisher is unverified — choose
   **More info → Run anyway**. (The app isn't code-signed yet; a certificate is
   on the roadmap.)
3. Launch LoreMonkey and sign in.

Updates are automatic: the app checks on launch and offers to install them.

## Requirements

- Windows 10 or 11 (64-bit)
- ~500 MB free space, plus room for the speech model downloaded on first use
- An NVIDIA GPU is optional — it enables larger, more accurate speech models

## This repository

Downloads only. The source lives in a private repository; nothing here but
release binaries and the update manifest.
